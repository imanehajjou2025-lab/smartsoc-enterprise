package com.smartsoc.domain.assets;

/** Nature d'un actif supervisé par le SOC. */
public enum AssetType {
    SERVER,
    WORKSTATION,
    NETWORK_DEVICE,
    DATABASE,
    APPLICATION,
    CLOUD_RESOURCE,
    OTHER
}
